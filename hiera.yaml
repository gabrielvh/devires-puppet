---
version: 5

defaults:
  datadir: hieradata

hierarchy:
  - name: "Env-specific"
    path: "%{::environment}"
  - name: "Host-specific"
    path: "%{::hostname}"
  - name: "Common defaults"
    path: "common.yaml"
